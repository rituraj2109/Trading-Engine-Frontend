# Backend API URL (set this in Vercel environment variables)
# Leave empty for local development (will use relative URLs)
# For production, set to your backend URL (e.g., https://your-backend.railway.app)
VITE_API_URL=
